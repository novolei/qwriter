# Qwriter 折叠导航栏验收 · 2026-09-07

final result: passed

## 设计依据与比较范围

- 用户确认第一款连续侧栏，随后确认缩窄版本：**56 CSS px 栏宽、18 px 图标、44 × 44 px 点击区域**。
- Source visual truth：`C:/Users/aresr/.codex/generated_images/01a0771f-2fad-7193-8395-d409ea062fa9/exec-a994ae1c-f9ba-49ff-9627-4746c6ecdbc3.png`，1344 × 1170 px。
- Implementation：[同尺寸截图](D:/Qwriter/docs/audit/navigation-rail/reference-state-1344x1170.png)，1344 × 1170 px，浏览器 CSS viewport 同尺寸，devicePixelRatio 约 1。全图无需密度缩放。
- 相同状态：浅色、中文、侧栏收起、同一示例文稿、右侧写作 Agent、知识库名称提示可见。实现使用键盘焦点显示提示，因此额外保留可访问焦点环；确认稿表现为悬停。
- 同一次视觉比较输入中打开了确认稿、最终实现全图及导航局部截图，检查整体结构与左侧品牌、四个入口、底部工具、提示框。核心比例以 DOM 测量和等尺寸全图为准；浏览器单独裁剪接口的截图有缩放差异，不用于像素尺寸判定。
- 生成稿中的栏宽约 83 px；使用已确认的 56 px 数值规格。生成稿放大了字体、工具栏和控件，实际实现保留产品既有字体、正文宽度、字号与工具栏；本次只落实侧栏方案。导航图标按 18 px 收细，纵向间距相应收紧。

## 发现、修复与复验

1. **[P2，已修复] 知识库首次加载关闭后焦点回到品牌按钮。** 懒加载期间替换整个 Modal，导致原入口丢失。复用现有 MemoryLibrary，打开时保持同一个弹窗生命周期；存储查询仍只在打开后执行。浏览器复验关闭后焦点回到“记忆与知识库”，应用集成测试覆盖此流程。
2. **[P2，已修复] 快速切换焦点短暂同时显示两个提示。** 初次同尺寸截图中可见速记和知识库提示并存。只对打开状态应用入场动画，关闭状态立即隐藏。再次执行相同键盘操作，最终同尺寸截图仅显示知识库提示；DOM 也仅有一个可见提示。
3. **[P2，已修复] 搜索列表辅助名称沿用第三方英文 Suggestions。** 改用 cmdk 的 `label` 协议属性接入 shared/i18n。新增断言捕获了 `aria-label` 会被第三方默认值覆盖的问题；最终测试及中文浏览器可访问树均显示“搜索结果”。
4. 初次栏内文稿图标与确认稿不同，改为现有 Lucide 的 BookOpenText；上部入口组增加留白，底部工具仍靠下锚定。最终对照图确认没有悬浮胶囊或双层导航头。

当前没有未解决的 P0/P1/P2 问题。

## 五项视觉检查

- **字体**：提示继承 Inter Variable / Noto Sans SC Variable，12 px、1.5 行高。正文仍使用用户所选字体；导航或语言切换不改写文稿。
- **间距**：栏宽实测 56 px；8 个按钮均为 44 × 44 px。品牌图标 32 px，普通图标 18 px / 1.5 线宽；按钮圆角 11 px，提示圆角 10 px，内边距 9 × 13 px。底部工具不遮挡正文或状态栏。
- **颜色**：复用 `--side`、`--green`、`--tint`、`--line`。浅色实测背景 rgb(241,241,234)，选中图标 rgb(73,102,77)；夜间随主题切换。键盘焦点独立可见。
- **资产**：复用官方羽毛 BrandIcon 与 Lucide 图标。没有生成替代品牌、位图导航、emoji 图标或重复字标。
- **文案**：图标提供名称与对应提示，新增文本在 zh-CN/en 资源中；用户文稿、品牌及模型名称保持原样。关闭弹窗后没有重复提示遗留。

## 实际交互与响应式检查

- 900 × 640：[收起状态](D:/Qwriter/docs/audit/navigation-rail/compact-900x640.png)、[抽屉](D:/Qwriter/docs/audit/navigation-rail/drawer-900x640.png)。图标栏仍为 56 px，整页无横向溢出；展开抽屉不再缩小编辑区。搜索获得焦点，Escape 或收起按钮关闭后返回原入口。
- 1344 × 1170：与确认稿同尺寸检查；宽屏展开侧栏后搜索获得焦点，收起后恢复图标栏。
- 1469 × 1270：常规预览中检查所有入口。搜索可以键入内容并用 Enter 选择文稿；速记可以打开、取消；知识库可以打开、关闭；设置与品牌菜单正常；Git 在浏览器中正确显示桌面版提示。
- 1920 × 1080：[高分辨率截图](D:/Qwriter/docs/audit/navigation-rail/desktop-1920x1080.png)。栏宽 56 px，整页宽度 1920 px，底部工具在可视区域内。
- 中英文、日间与夜读：[英文夜间截图](D:/Qwriter/docs/audit/navigation-rail/en-dark-900x640.png)。英文提示不截断，语言切换不影响文稿。
- 专注模式隐藏侧栏和图标栏；退出后恢复导航。提示支持 Tab 焦点和 Escape；指针提示不执行导航，由组件交互测试覆盖。自动化浏览器没有单独移动指针接口，未将键盘提示截图冒充鼠标悬停实测。
- `prefers-reduced-motion` 已禁用导航和提示入场动画，按钮关闭过渡；本次浏览器未仿真该媒体偏好。
- 本次最终浏览器 error 日志为空。原有文稿、卡片未增删；验收后恢复原文稿、中文、浅色、快速协作和默认 viewport，并保留折叠栏供用户体验。

## 工程与边界

- NavigationPanel 负责应用组合，NavigationRail 只接收小型回调；Tooltip 复用 Radix，不引入手写浮层定位。新增手写文件均低于 150 行；App.tsx 收敛为 359 行。
- `npm run check`：51 个前端测试文件、121 项测试通过；54 项 Rust 测试通过，4 项既有真实环境测试按原规则忽略。架构、i18n、格式、严格 TS、Rust fmt/clippy 和 IPC 漂移检查通过。
- Windows 桌面构建验证单独执行，产物 `src-tauri/target/debug/qwriter.exe`；本次没有改 Rust、IPC、权限或数据格式。
- 本轮为 Windows 浏览器交互与 Windows 构建验证。macOS 原生运行未验证；Git 原生提交、系统截图和在线模型调用不属于此次侧栏交互验收。
- 验收截图为本地留存，按仓库规则忽略，不随源码提交。

## 后续可选打磨

无阻塞项。可在下一轮依据真实使用反馈决定是否允许自定义图标入口顺序；本次保持确认稿的信息结构。
