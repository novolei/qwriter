# 模型备份与迁移验收

日期：2026-09-07。平台：Windows，独立 Chrome 浏览器测试会话；macOS 原生交互未验证。

## 行为

- 浏览器实际导出 `Qwriter-models-2026-09-07.json`，读取确认仅有公开配置（合成默认 Ollama 配置，488 bytes）。
- 将该文件通过真实文件输入重新导入；默认显示重复项并禁用确认，模型池未改变。
- 切换为独立副本，确认后供应商与模型数量从 1 变为 2，保留原当前模型；刷新后数据仍保留。
- 中英切换通过外观设置操作；深色通过 Night · Forest 切换。
- 1440×900、1920×1080、900×640 已截图检查。常规/大屏设置外框 600×840；小窗口约 600×602，外观与模型页面相同，无横向溢出，正文可滚动。
- 控制台无应用错误，未发现 Vite 错误遮罩。

## 实测发现并修复

900×640 下，已有的文稿库收起提示（z-index 24）盖住设置弹窗（20）。将背景提示层级调整为 18，重新加载开发服务后，实际计算样式与截图确认提示正确位于弹窗遮罩下。

测试工具的 `download` 命令在本机两次返回 canceled；普通点击配合明确的 Windows download-path 成功落盘。保留这一工具限制记录，没有将它误报为应用已保存成功。

## 自动回归

新增 9 项测试，覆盖：字段白名单/凭据排除、恶意地址与损坏文件、长度/引用/重复约束、重复和重名处理、新本机 ID、选中模型保留、导入预览/取消、保存失败不修改原池且可重试、陈旧读取、英文错误恢复。

界面测试与原生文件保存不同；本轮浏览器验证不等于 macOS 原生保存对话框及系统凭据真机验证。现有已发布的 0.2.0-preview.1 安装包不自动包含本轮修改。
